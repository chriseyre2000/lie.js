{
  "name": "lie.js",
  "version": "1.1.8",
  "description": "This adds useful helper functions to promises.",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "scripts": {
    "test": "npm run build && mocha",
    "build": "tsc src/index.ts -d --outDir ./lib",
    "format": "prettier --write \"src/**/*.ts\"",
    "lint": "eslint",
    "prepare": "npm run build",
    "prepublishOnly": "npm test && npm run lint"
  },
  "files": [
    "lib/**/*"
  ],
  "author": "chriseyre2000",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/chriseyre2000/lie.js.git"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/es6-shim": "^0.31.40",
    "chai": "^4.2.0",
    "eslint": "^7.4.0",
    "mocha": "^9.0.0",
    "prettier": "^2.0.5",
    "tslint-config-prettier": "^1.18.0",
    "typescript": "^4.0.2"
  }
}
